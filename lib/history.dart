import 'package:flutter/material.dart';

class ACHistory extends StatefulWidget {
  const ACHistory({Key? key}) : super(key: key);

  @override
  State<ACHistory> createState() => _ACHistoryState();
}

class _ACHistoryState extends State<ACHistory> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
